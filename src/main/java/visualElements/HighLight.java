package visualElements;
/**
 * this enum class is for determine states of highlight
 * -> PLAY: highlight during playing
 * -> SELECTED: highlight when user select it
 * -> NULL: do nothing. default states
 * @author Kuimou Yu
 * */
public enum HighLight {
	PLAY,SELECTED,NULL
}
